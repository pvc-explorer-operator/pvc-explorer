apiVersion: v1
kind: Service
metadata:
  name: pvc-explorer-controller
  labels:
    app: pvc-explorer
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: http
      nodePort: 30080
      protocol: TCP
      name: http
  selector:
    app: pvc-explorer
