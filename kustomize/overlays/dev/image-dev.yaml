apiVersion: apps/v1
kind: Deployment
metadata:
  name: pvc-explorer-controller-manager
spec:
  template:
    spec:
      containers:
        - name: manager
          image: pvc-explorer:dev
          # Never pull from registry — image is always loaded locally via kind load
          imagePullPolicy: Never
