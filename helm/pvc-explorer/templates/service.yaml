apiVersion: v1
kind: Service
metadata:
  name: pvc-explorer-controller
  labels:
    app: pvc-explorer
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: pvc-explorer
