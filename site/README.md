# Docs Site

This directory contains the VitePress docs site used for published documentation pages.

## Local development

```bash
cd site
npm install
npm run dev
```

## Build

```bash
cd site
npm run build
```

Canonical operational docs live in `../docs/`. The site may include adapted pages for web-only presentation.