---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: pvcexplorerscopes.pvcexplorer.io
spec:
  group: pvcexplorer.io
  names:
    kind: PVCExplorerScope
    listKind: PVCExplorerScopeList
    plural: pvcexplorerscopes
    shortNames:
    - pvcs
    singular: pvcexplorerscope
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.namespaceCount
      name: Namespaces
      type: integer
    - jsonPath: .status.explorerCount
      name: Explorers
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          PVCExplorerScope is the Schema for the pvcexplorerscopes API.
          A scope registers one or more namespaces for PVC exploration and provides
          default configuration for all PVCExplorer agents it creates.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PVCExplorerScope.
            properties:
              defaults:
                description: defaults provides default values applied to every PVCExplorer
                  created by this scope.
                properties:
                  forceRW:
                    default: true
                    description: |-
                      forceRW, when true, mounts the PVC read-write when no consumers are present.
                      Automatically overridden to read-only when consumers are detected.
                    type: boolean
                  image:
                    description: image is the default container image for agent pods.
                    type: string
                  mode:
                    default: ScaledToZero
                    description: mode is the default deployment mode for agents.
                    enum:
                    - ScaledToZero
                    - Deployment
                    type: string
                  mountStrategy:
                    description: mountStrategy holds the default mount strategy options.
                    properties:
                      allowNodeAffinity:
                        default: true
                        description: |-
                          allowNodeAffinity permits the controller to set nodeAffinity on agent pods
                          when the PVC is RWO and consumed by another pod. Defaults to true.
                        type: boolean
                      fallbackOnConflict:
                        default: Pending
                        description: |-
                          fallbackOnConflict controls agent behaviour when a RWO PVC has consumers on
                          multiple nodes. Currently only Pending is supported.
                        enum:
                        - Pending
                        type: string
                    type: object
                  resources:
                    description: resources defines default compute resource requests/limits
                      for agent pods.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  scaling:
                    description: scaling holds the default idle/startup timeouts.
                    properties:
                      idleTimeout:
                        default: 10m
                        description: idleTimeout is the duration an agent may remain
                          idle before being scaled to zero.
                        type: string
                      startupTimeout:
                        default: 60s
                        description: |-
                          startupTimeout is the maximum time to wait for the agent pod to pass its
                          /healthz probe after being woken.
                        type: string
                    type: object
                type: object
              deletionPolicy:
                default: Cleanup
                description: |-
                  deletionPolicy controls what happens to owned PVCExplorer CRs when this scope
                  is deleted. PVCs are never touched.
                enum:
                - Cleanup
                - Orphan
                type: string
              discovery:
                description: discovery controls how PVCs are discovered within the
                  registered namespaces.
                properties:
                  excludePVCs:
                    description: |-
                      excludePVCs is a list of PVC name patterns (glob syntax) to exclude even
                      in Auto mode.
                    items:
                      type: string
                    type: array
                  mode:
                    default: Auto
                    description: mode is Auto (all PVCs) or Explicit (only pvcNames).
                    enum:
                    - Auto
                    - Explicit
                    type: string
                  pvcNames:
                    description: pvcNames is only used when mode is Explicit.
                    items:
                      type: string
                    type: array
                required:
                - mode
                type: object
              namespaces:
                description: namespaces selects which namespaces are registered in
                  this scope.
                properties:
                  labelSelector:
                    description: |-
                      labelSelector registers namespaces that match this label selector.
                      Combined with names using OR logic.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  names:
                    description: names is a static list of namespace names to register.
                    items:
                      type: string
                    type: array
                type: object
            required:
            - namespaces
            type: object
          status:
            description: status defines the observed state of PVCExplorerScope.
            properties:
              conditions:
                description: conditions represent the current state of the PVCExplorerScope
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              explorerCount:
                description: explorerCount is the number of PVCExplorer CRs owned
                  by this scope.
                type: integer
              namespaceCount:
                description: namespaceCount is the number of namespaces currently
                  registered by this scope.
                type: integer
              observedGeneration:
                description: |-
                  observedGeneration is the .metadata.generation that the status was last
                  reconciled against.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
