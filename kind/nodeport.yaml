apiVersion: v1
kind: Service
metadata:
  name: pvc-explorer-controller
  namespace: pvc-explorer-system
spec:
  type: NodePort
  selector:
    app: pvc-explorer
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
